{
  "name": "basecamp-issue-provider",
  "version": "0.1.0",
  "description": "Basecamp To-dos issue provider plugin for Super Productivity",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "package": "npm run build && cd dist && zip -r ../basecamp-issue-provider.zip . && cd .."
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.0"
  }
}
